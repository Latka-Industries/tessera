//! Deterministic golden / sample `.tes` builders.
//!
//! Shared by `examples/gen_v0_fixtures`, `examples/gen_vault_fixtures`, and
//! `src/tests/golden_v0.rs` so on-disk bytes stay single-sourced.

pub mod v0;
pub mod vault;
