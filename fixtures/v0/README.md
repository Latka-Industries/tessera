# Golden `.tes` fixtures (planned)

Populated when [THI-8](https://linear.app/thicclatka/issue/THI-8) lands:

| File | Description |
| --- | --- |
| `empty.tes` | Superblock only |
| `note_one_chunk.tes` | Single paragraph note |
| `note_three_chunks.tes` | Heading + two paragraphs |
| `hub_links.tes` | Hub doc + link table |

Regenerate via `cargo run --example gen_v0_fixtures` (TBD).
