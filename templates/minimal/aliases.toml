# Fixed-string aliases expanded as `\name` → value (D23 / THI-354).
# Names are ASCII identifiers. `\name{…}` is left alone (phrase/font territory).

[aliases]
# Example only — packs override with real shortcuts.
# maryamlatin = "Maryam"
