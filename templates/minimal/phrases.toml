# Parameterized phrases: `\phrase{key}` / `\phrase{key}{arg…}` (D23 / THI-355).
# Keys are ASCII identifiers. Slots: `{arg}` / `$1` (first) and `{argN}` / `$N`.
# Markdown in the template is fine (*emphasis*); sealed `.tes` stores ordinary prose.

[phrases]
yegourdoon = "*{arg}*"
